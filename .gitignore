# Python
__pycache__/
*.pyc
*.pyo
.venv/
venv/

# Editors
.vscode/
.idea/
*.swp

# OS
.DS_Store
Thumbs.db

# Local board data — don't ship user boards in the skill repo
board/
*.local.json

# Telemetry logs (regenerate per install)
telemetry/events.jsonl

# Conversation-derived training data — private, never commit
training_data/
board/_digest_*.txt

# Added by board-steward — local board data may contain secrets
board/board.json
board/index.json
board/extraction_snapshot.json
board/recon_pending.json

# board server runtime log
.board-server.log
.board-recon.log

# local plugin-reinstall backups (machine state, never ship)
.reinstall-backups/

# Maintainer-only dev tooling — tests, sims, benchmarks, git-hooks, session
# loggers. Never ship to the public plugin/marketplace (it's "dev", not runtime).
dev/

# Added by board-steward — local board data may contain secrets
board/.replay_state.json

# local README preview (gitignored — local-only convenience)
README.preview.html

# Research benchmark bulk data — large downloaded datasets (BEIR/LOCOMO) +
# OpenAI embedding caches (~250MB). Reproducible from the harness scripts (see
# each Research/*/README.md), so the CODE + REPORTS are tracked, the data is not.
Research/global-benchmarks/inputs/
Research/global-benchmarks/results/dense_cache*.json
Research/retrieval-bakeoff/results/dense_cache.json
